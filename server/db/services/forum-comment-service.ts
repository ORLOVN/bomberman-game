import { BaseRESTService } from "./base-rest-service";
// import { User } from "../models/user";
// import { ETheme } from "../models/enum";
// import { Comments } from "../models/forum/comments";

export class ForumCommentService implements BaseRESTService {
  // public find = async (topicId: string) => Comments.findAll({ where: { topicId } });
  // public create = async (yaId: number) => {
  //   const user = await this.find(yaId);

  //   const newTheme = user.theme === ETheme.LIGHT ? ETheme.DARK : ETheme.LIGHT;

  //   User.update(
  //     {
  //       theme: newTheme,
  //     },
  //     { where: { yaId } }
  //   );

  //   return Promise.resolve({ theme: newTheme });
  // };
}
